# Business Operations pack example

This synthetic example contains exactly the nine configured proposal artifact
slots. `work-accord.json` binds exact paths, policy, registry, Phase Contracts,
limits, prohibited effects, role separation, and human gates.

The hermetic test compiles all five Phase Contracts, applies fake Kernel
authorization, redeems a run-bound CAS operation grant for each model/repository
operation, and runs authoritative DLP before context and packaging. It reviews
and revises the artifact set, packages it in isolated local Git, binds the
resulting commit head to COMMENT review and human wait, then validates four
distinct purpose-scoped business
owner authorizations and approvals. A signed independent merge observation
permits repository-only proposal closure. No merge mutation method exists, and
every CRM, ERP, ticketing, payment, procurement, communication, policy, and
production adapter fake records zero calls.
