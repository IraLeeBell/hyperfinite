# Feature Delivery demo

This synthetic, network-free example follows the exact reserved journey:

`Intake -> Requirements clarification -> Codebase discovery -> Solution design -> Implementation plan -> Build -> Test and verification -> Human review -> Completed`

The checked-in activation profile is disabled. The example records
`example-organization/hyperfinite` and base
`8f594d53db291f6bb2803f26d421a70ced556362` as inert synthetic evidence, not a
current live target. Issue text and observed paths grant no authority. Build
output contains only `export-module` and `export-test` logical slots. Trusted
code alone maps those slots to exact paths and may create only a draft pull
request.

The hands-off fixture stops at Human Review after all fixed commands and a
`COMMENT`-only review. `synthetic-human-continuation.json` demonstrates a
separate eligible-human merge observation reaching Completed without granting
automation approval or merge authority. The closed external-call assertion
fixture contains zero for all four categories; it is not runtime telemetry.
