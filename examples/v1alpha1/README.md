# Baseline v1alpha1 example

`work-accord.json` is the shared synthetic Work Accord used by core validation
and tests. It is not bound to a live repository, issue, Project, credential, or
human authorization.

Demo- and domain-specific examples use their own directories under
[`examples/`](../README.md).
