# Marketing pack example

This synthetic example contains exactly the eight configured repository slots.
`work-accord.json` binds the exact paths, policy, registry, Phase Contracts,
limits, prohibited effects, and human gates. The content is original synthetic
fixture data.

The hermetic test compiles all five Phase Contracts, applies fake Kernel
authorization, redeems a run-bound CAS operation grant for each model/repository
operation, and runs authoritative DLP before context and packaging. It resolves
structured claims and each complete draft/asset scope plus rights to exact signed
evidence, packages a revised artifact set in isolated local Git, binds the
resulting commit head to COMMENT review and human wait, then accepts separate
purpose-scoped brand/legal actor authorizations and approvals. A signed
independent merge observation permits repository-only closure. No merge or
publication mutation method exists, and every external adapter fake records zero
calls.
